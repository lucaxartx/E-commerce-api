const customError = require("../errors");

const register = async () => {
  res.send("register");
};

const login = async () => {
  res.send("register");
};
const logout = async () => {
  res.send("register");
};

module.exports = { register, login, logout };
